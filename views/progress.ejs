<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Progress - <%= appName %></title>
    <link rel="stylesheet" href="/main.css">
    <style>
        body {
            font-family: Arial, sans-serif;
            text-align: center;
            background-color: #f4f4f9;
            color: #333;
        }
        h1, h2 {
            text-align: center;
            color: rgb(116, 0, 139);
        }
        .progress-container {
            max-width: 800px;
            margin: 20px auto;
            padding: 10px;
        }
        .assignment-list {
            list-style: none;
            padding: 0;
        }
        .assignment-item {
            display: flex;
            justify-content: space-between;
            align-items: center;
            border: 1px solid #ccc;
            border-radius: 5px;
            margin-bottom: 10px;
            padding: 10px;
            background-color: #fff;
            box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1);
        }
        .assignment-item.completed {
            background-color: #d4edda; /* Green for completed */
        }
        .assignment-item.in-progress {
            background-color: #ffef96; /* Yellow for in-progress */
        }
        .assignment-title {
            font-weight: bold;
        }
        .assignment-due-date {
            font-size: 0.9em;
            color: #555;
        }
    </style>
</head>
<body>
    <h1>Assignment Progress</h1>
    <div class="progress-container">
        <h2>In Progress</h2>
        <% if (inProgress && inProgress.length > 0) { %>
            <ul class="assignment-list">
                <% inProgress.forEach(function(assignment) { %>
                    <li class="assignment-item in-progress">
                        <span class="assignment-title"><%= assignment.title %></span>
                        <span class="assignment-due-date">Due: <%= new Date(assignment.dueDate).toLocaleDateString() %></span>
                    </li>
                <% }); %>
            </ul>
        <% } else { %>
            <p>No assignments in progress.</p>
        <% } %>

        <h2>Completed</h2>
        <% if (completed && completed.length > 0) { %>
            <ul class="assignment-list">
                <% completed.forEach(function(assignment) { %>
                    <li class="assignment-item completed">
                        <span class="assignment-title"><%= assignment.title %></span>
                    </li>
                <% }); %>
            </ul>
        <% } else { %>
            <p>No completed assignments.</p>
        <% } %>        
    </div>
    <a href="/">Back to Home</a>
    <a href="/welcome?username=Lily%20Hampton">Go to Welcome Page Lily </a> 
    <a href="/welcome?username=student1">Go to Welcome Page Student1 </a> 
</body>
</html>
